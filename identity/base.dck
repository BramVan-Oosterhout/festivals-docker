# docker image for base install if festivals-database

FROM ubuntu:22.04

ARG DEBIAN_FRONTEND=noninteractive

ARG APP='Festivals-App'
WORKDIR /home/build

RUN apt-get -y  update
RUN apt-get -y install curl sudo ufw systemctl apt-utils
#sudo ufw php8.1-fpm nginx systemctl joe less iproute2

################## Database Server ###################
COPY install.sh install.sh
RUN chmod +x install.sh
RUN ./install.sh insecure insecure insecure 

