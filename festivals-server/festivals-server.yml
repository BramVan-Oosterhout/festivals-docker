# compose file for festivals-server
version: '1'

services:

  identity:
    restart: always
    image: my/festivals-server
    container_name: festivals-server
    hostname: festivals-server
    networks:
      festivals_backend:
 #       ipv4_address: 172.20.0.1
      
networks:
  festivals_backend:
    external: true
    name: festivals-network
